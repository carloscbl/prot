#include "discovered_components.h"
// #include "db_ops.hpp"

bool discovered_components::build(){
    // this->m_user = read_user(user_id);
    // this->m_app = move(read_app_by_id(app_id)->first);

    return true;
}