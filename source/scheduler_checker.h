#ifndef SCHEDULER_CHECKER_H
#define SCHEDULER_CHECKER_H

//TODO : Move here the algorithm of slot checker to find if a task meet requirements to be allocated 
class scheduler_checker
{
private:
    /* data */
public:
    scheduler_checker(/* args */);
    ~scheduler_checker();

};

scheduler_checker::scheduler_checker(/* args */)
{
}

scheduler_checker::~scheduler_checker()
{
}

// bool space_today(){
//     if(current_it_interval.lower() > end_of_day){
//             return nullopt; //-> Skip this day
//         }
// }


#endif //SCHEDULER_CHECKER_H